get:
  summary: Retrieve a list of available models
  description: Fetches a list of all models in the system. 
  operationId: find_all
  tags:
    - Model
  responses:
    '200':
      description: A list of models
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './DTO/modelDTO.yml'
